# Repository Context

Generated context bundles live here and should not be committed.

Suggested flow:

1. Review `repomix.config.json`.
2. Generate a context bundle with the approved local tool.
3. Pair the bundle with a prompt from `.opendock/prompts/`.
4. Delete stale bundles after the session.
